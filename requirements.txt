# e-YMM Karşıt İnceleme Asistanı — bağımlılıklar
pandas
openpyxl
xlrd          # eski .xls dosyaları için
pillow        # GUI logosu için

olefile       # hazır .doc (eski ikili) şablonlardan VKN okuma
python-docx   # .docx şablon okuma VE yazma (Word gerektirmeden tutanak üretir)

# Opsiyonel — kurulu değilse program çalışır, sadece ilgili özellik devre dışı:
reportlab     # "PDF kopya da üret" seçeneği için
pywin32 ; platform_system == "Windows"   # yalnızca eski .doc şablonlardan üretim için (Windows + Word)

# Geliştirme / test:
pytest
